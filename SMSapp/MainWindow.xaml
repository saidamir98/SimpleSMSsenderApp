<Window x:Class="SMSapp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMSapp"
        mc:Ignorable="d"
        Title="MainWindow" ResizeMode="NoResize" Height="285" Width="220">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="MessageTextBox" Margin="0 20" TextWrapping="Wrap" Grid.Row="0" Width="180" Height="180">GSM SMS MESSAGE FROM C# APP!)</TextBox>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TelNumBox" Grid.Column="0">+998943614198</TextBox>
            <Button Grid.Column="1" Click="btnSend" >Send</Button>
        </Grid>
        
    </Grid>
</Window>
